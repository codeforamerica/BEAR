// @flow
import React, { Component } from 'react';
import { Document, Page, Text } from '@react-pdf/renderer';

type Props = {
  county: string,
  dateTime: string
};

export default class SummaryReportPdf extends Component<Props> {
  render() {
    const { county, dateTime } = this.props;
    return (
      <Document>
        <Page>
          <Text className={styles.header}>Summary Report: {county}</Text>
          <Text className="small--text">
            This report was generated by Clear My Record on {dateTime} for
            {county} County
          </Text>
          <Text className={styles.subtitle}>
            Background on the file that California DOJ provided to {county}
            County:
          </Text>
          <Text className="small--text">
            DOJ provided a spreadsheet with the entire CA criminal record
            history for every individual convicted of H&S §11357, H&S §11358,
            H&S §11359, and/or H&S §11360 in {county} County since December
            1973.
          </Text>
          <Text
            style={styles.pageNumber}
            render={({ pageNumber, totalPages }) =>
              `${pageNumber} / ${totalPages}`
            }
            fixed
          />
        </Page>
      </Document>
    );
  }
}

const styles = StyleSheet.create({
  body: {
    paddingTop: 35,
    paddingBottom: 65,
    paddingHorizontal: 35
  },
  title: {
    fontSize: 24,
    textAlign: 'center',
    fontFamily: 'Oswald'
  },
  author: {
    fontSize: 12,
    textAlign: 'center',
    marginBottom: 40
  },
  subtitle: {
    fontSize: 18,
    margin: 12,
    fontFamily: 'Oswald'
  },
  text: {
    margin: 12,
    fontSize: 14,
    textAlign: 'justify',
    fontFamily: 'Times-Roman'
  },
  image: {
    marginVertical: 15,
    marginHorizontal: 100
  },
  header: {
    fontSize: 12,
    marginBottom: 20,
    textAlign: 'center',
    color: 'grey'
  },
  pageNumber: {
    position: 'absolute',
    fontSize: 12,
    bottom: 30,
    left: 0,
    right: 0,
    textAlign: 'center',
    color: 'grey'
  }
});
